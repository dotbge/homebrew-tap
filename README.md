# dotbge Homebrew tap

```bash
brew install --cask dotbge/tap/bge
```

Casks for [dotbge](https://dotbge.com) command-line tools. Source for `bge` ships as a
signed, notarized build from [dotbge/bge-cli](https://github.com/dotbge/bge-cli/releases).
